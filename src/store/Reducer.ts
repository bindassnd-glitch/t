import { initialStateType } from "@/types/Reducer.types";

export const initialState: initialStateType = {} as initialStateType;

export const reducer = (state: initialStateType): initialStateType => {
    return state;
}

export default reducer;